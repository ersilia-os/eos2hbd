# Model pretrained parameters

This model was validated with a 5-Fold CV resulting in a mean squared error (MSE) of 0.346 ± 0.005. 
The final model was then trained on the full dataset, achieving an MSE of 0.048.
